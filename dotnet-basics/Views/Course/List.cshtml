@model List<Course>

    <h1 class="mb-3 h3">Tüm Kurslar</h1>

<div class="row">

    @foreach (Course course in Model)
    {

        @if (course.IsActive)
        {
            <div class="col-md-3">
                @await Html.PartialAsync("_CourseCard", course)
            </div>
            
        }
    }
</div>